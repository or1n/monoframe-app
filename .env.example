# Example environment variables (do not commit real secrets)
# Replace values before deploying

# Public site URL (used for canonical links)
NEXT_PUBLIC_SITE_URL=https://example.com

# If you add analytics or third-party keys, expose only public ones as NEXT_PUBLIC_*
# Example:
# NEXT_PUBLIC_ANALYTICS_ID=YOUR_ID

# Backend/API secrets must NOT be committed
# Example (server-only, never expose to client):
# SECRET_API_KEY=your-secret-value
